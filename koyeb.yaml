name: 𝗠𝗨𝗭𝗔𝗠𝗠𝗜𝗟_𝗠𝗗
services:
  - name: muzammil_md-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
